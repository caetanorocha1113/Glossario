import { initScrollReveal } from "./scrollReveal.js";
import { hoverChangeDescription } from "./htm.js";
import { menu } from "./html.js";

menu();
initScrollReveal();

hoverChangeDescription(
  ".html",
  "Representa um conteúdo independente e autocontido, que pode ser distribuído e reutilizado de forma isolada, como posts de blog, artigos de jornal ou cards de produto. Geralmente contém título, texto, imagens e outros elementos relacionados ao mesmo assunto."
);
hoverChangeDescription(
  ".css",
  "Permite oferecer diferentes versões da mesma imagem, adaptando-se ao tamanho da tela ou resolução do dispositivo. Muito usado para imagens responsivas."
);
hoverChangeDescription(
  ".js",
  "Contém código HTML que não é exibido imediatamente na página. É usado como modelo para ser inserido no DOM via JavaScript."
);
hoverChangeDescription(
  ".sass",
  "Fornece arquivos de mídia alternativos para <audio>, <video> ou <picture>. O navegador escolhe qual usar dependendo do formato ou condição especificada."
);
hoverChangeDescription(
  ".react",
  "Define metadados da página, como codificação, autor, descrição, palavras-chave e informações para SEO ou redes sociais. Fica no <head> e não é visível para o usuário."
);
hoverChangeDescription(
  ".next",
  "Cria uma área em branco para desenhar gráficos, animações ou imagens usando JavaScript. Muito usado para jogos e visualizações dinâmicas."
);
hoverChangeDescription(
  ".styled",
  "Exibe uma barra de progresso para indicar a conclusão de uma tarefa, como downloads ou carregamentos."
);
hoverChangeDescription(
  ".tailwind",
  "Incorpora conteúdo externo, como arquivos PDF, vídeos, aplicativos Flash ou outro HTML. É mais antigo e menos usado, mas ainda funcional."
);
hoverChangeDescription(
  ".typescript",
  "Cria uma seção expansível/retrátil que o usuário pode abrir ou fechar para revelar mais conteúdo."
);
hoverChangeDescription(
  ".radix",
  "Destaca texto importante ou relevante visualmente, geralmente com um fundo amarelo por padrão."
);
hoverChangeDescription(
  ".cypress",
  "Representa um valor dentro de um intervalo conhecido, como níveis de bateria, avaliações ou pontuações."
);
hoverChangeDescription(
  ".storybook",
  "Define abreviações ou siglas e fornece o significado completo quando o usuário passa o mouse sobre o texto."
);

