import { initScrollReveal } from "./scrollReveal.js";
import { hoverChangeDescription } from "./htm.js";
import { menu } from "./html.js";

menu();
initScrollReveal();
hoverChangeDescription(
  ".aaa",
  "Estrutura fundamental em Java que serve como molde para criar objetos, definindo atributos (dados) e métodos (ações)."
);
hoverChangeDescription(
  ".bbb",
  "Ponto de entrada da aplicação Java; o método principal que o programa executa ao iniciar."
);
hoverChangeDescription(
  ".ccc",
  "Comando para imprimir texto no console, usado para exibir mensagens ou resultados."
);
hoverChangeDescription(
  ".ddd",
  "Tipos de dados básicos e primitivos (exceto String), usados para armazenar diferentes tipos de valores."
);
hoverChangeDescription(
  ".eee",
  "Estrutura condicional que executa blocos de código diferentes dependendo de uma condição verdadeira ou falsa."
);
hoverChangeDescription(
  ".fff",
  "Estruturas de repetição usadas para executar um bloco de código várias vezes, com base em uma condição ou contagem."
);
hoverChangeDescription(
  ".ggg",
  "Permite trazer classes e pacotes externos para o código, evitando reescrever funcionalidades já existentes."
);
hoverChangeDescription(
  ".hhh",
  "Palavra-chave usada para criar novos objetos a partir de classes."
);
hoverChangeDescription(
  ".iii",
  "Referência ao próprio objeto atual, útil para diferenciar variáveis de instância de variáveis locais."
);
hoverChangeDescription(
  ".jjj",
  "Indica herança, permitindo que uma classe herde atributos e métodos de outra."
);
hoverChangeDescription(
  ".kkk",
  "Indica que uma classe está implementando uma interface, comprometendo-se a definir seus métodos."
);
hoverChangeDescription(
  ".lll",
  "Anotação usada para indicar que um método está sobrescrevendo outro de uma classe pai ou interface."
);